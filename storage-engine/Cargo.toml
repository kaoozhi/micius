[package]
name = "storage-engine"
version = "0.1.0"
edition = "2024"

[dependencies]
# Async runtime
tokio          = { version = "1", features = ["full"] }

# gRPC
tonic          = "0.11"
prost          = "0.12"
anyhow = "1.0.102"

[build-dependencies]
tonic-build = "0.11"
